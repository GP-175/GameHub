{
  "name": "gamehub-gp-hoot",
  "version": "1.0.0",
  "description": "GameHub with GP-hoot, a live team quiz game.",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('GameHub static assets require no build step')\"",
    "start": "node server/server.js",
    "test": "node --test",
    "load:gp-hoot": "node scripts/load-gp-hoot.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "qrcode": "^1.5.4",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
